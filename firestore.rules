rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /shared_products/{id} { allow read, write: if request.auth != null; }
    match /shared_state/{id} { allow read, write: if request.auth != null; }
  }
}
